[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sba"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sba = "sba.__main__:app"

[tool.setuptools.packages.find]
where = ["src"]